function testFunction(executionContext){
    try{
        debugger;
        var formContext = executionContext.getFormContext();

        var testAttr = formContext.getAttribute("cr463_testfield").getValue();
        if(testAttr){
            var websiteAttr = formContext.getAttribute("websiteurl");
            var emailAttr = formContext.getAttribute("emailaddress1");
        }
        else{
            var emailAttr = formContext.getAttribute("emailaddress1");
        }

    }
    catch(e){
        console.log("Error in testFunction, e")
    }
}